# Next.js website origin (no trailing slash). Simulator: http://127.0.0.1:3000 — physical device: your machine LAN IP.
EXPO_PUBLIC_API_URL=http://127.0.0.1:3000

# Dev only: Prisma `User.id` sent as `x-kairo-user-id` for mutating API routes until Clerk is wired on the website.
# EXPO_PUBLIC_KAIRO_DEV_USER_ID=
