# Used by Next.js and Prisma when DATABASE_URL is loaded from this app root.
# Copy to `.env` and adjust credentials. For CLI migrations, you can also set
# this in `packages/db/.env` or run commands from the repo root with a root `.env`.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/kairo"
