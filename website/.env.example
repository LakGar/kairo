# PostgreSQL for Prisma (`@kairo/db`). Copy to `.env` or `.env.local`.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/kairo"

# Optional — local API dev user (see `website/src/lib/current-user.ts`).
# Client sends: x-kairo-user-id: <existing User.id cuid>
# X_KAIRO_USER_ID_HEADER=x-kairo-user-id
